@isTest(seeAllData=True)
public class SnowFlakeControllerTest {
    @IsTest static void testSnowFlakeControllerTest() {
        /*Test.startTest();

        Test.setMock(HttpCalloutMock.class, new SnowFlakeControllerMock());
        SnowFlakeController.getTimelineEntries('test');

        Test.stopTest();*/
    }
}